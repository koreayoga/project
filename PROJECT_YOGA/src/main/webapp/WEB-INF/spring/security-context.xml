<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security 
	                    http://www.springframework.org/schema/security/spring-security.xsd
		                http://www.springframework.org/schema/beans 
		                http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="customAD" class="org.zerock.security.CustomAccessDeniedHandeler"/>	
	<bean id="customLS" class="org.zerock.security.CustomLoginSuccessHandeler"/>
	<bean id="BcryptPE" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>	                
	<bean id="customUS" class="org.zerock.security.CustomUserDetailsService"/>
	
	<security:http>
		<security:intercept-url pattern="/sample/all" access="permitAll"/>
		<security:intercept-url pattern="/sample/member" access="hasRole('MEMBER')"/>
		<security:intercept-url pattern="/sample/admin" access="hasRole('ADMIN')"/>
		<!-- <security:access-denied-handler error-page="/accessError"/> -->
		<security:access-denied-handler ref="customAD"/>
		<security:intercept-url pattern="/board/register" access="isAuthenticated()" />
		
		<security:form-login login-page="/loginout/login"/>
		<!-- <security:form-login login-page="/customLogin" authentication-success-handler-ref="customLS"/> -->				
		<!-- 로그인시 자동로그인 설정 -->
		<security:remember-me data-source-ref="dataSource" token-validity-seconds="604800"/>
		<!-- 기본 로그아웃 -->
		<!-- <security:logout logout-url="/customLogout" invalidate-session="true"/> -->
		<!-- 로그아웃시 자동로그인 종료 -->
		<security:logout logout-url="/loginout/logout" invalidate-session="true" delete-cookies="remember-me, JSESSION_ID"/>
	</security:http>
	
	<security:authentication-manager>
		<!-- 사용자 정보 조회 및 처리 -->
		<!-- <security:authentication-provider> -->
		<security:authentication-provider user-service-ref="customUS">
		<!-- DB데이터 이용 로그인 권한 설정 -->
			<!-- <security:jdbc-user-service data-source-ref="dataSource"/> -->
			<!-- CustomUserDetailService를 이용하려면 아래 jdbc-user-service사용불가 -->
			<!-- <security:jdbc-user-service data-source-ref="dataSource" 
				users-by-username-query="select userid username, userpw password, enabled from tbl_member where userid = ?" 
				authorities-by-username-query="select userid username, auth authority from tbl_member_auth where userid = ?"/> -->
		<!-- Password Encoding -->
			<!-- 1.custom -->
			<!-- <security:password-encoder ref="customPE"/> -->
		<!-- 2. BCryptPasswordEncoder -->
			<security:password-encoder ref="BcryptPE"/>
			
		<!-- 하드코딩 -->
			<!-- <security:user-service>
				<security:user name="kevinyh" password="{noop}1111" authorities="ROLE_MEMBER"/>
				<security:user name="admin" password="{noop}2222" authorities="ROLE_MEMBER, ROLE_ADMIN"/>
			</security:user-service> -->
		</security:authentication-provider>
	</security:authentication-manager>

</beans>